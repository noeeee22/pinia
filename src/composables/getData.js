import { ref } from 'vue'
import { Axios } from 'axios'

export const useGetData  ={

    // const getData = async (url) => {
    //     const data= resf(null)
    //     try {
    //         const res = await axios.get(url)
    //         poke.value = data.results
    //     } catch (error) {
    //         console.log(error);
    //     }
    // }


}